# Rootline redesign QA · 2026-09-08

final result: passed

## 比较基准与证据

- source visual truth: `/Users/shengshuyuan/.codex/visualizations/2026/09/08/01a07e9a-f4c2-7a22-b38c-0274d985a404/design-1.png`
- implementation: `docs/redesign/desktop.png`，实际浏览器 `http://127.0.0.1:43827/`。
- viewport: 桌面 1440×1024 CSS px，手机 390×844 CSS px；截图像素尺寸分别与 CSS 尺寸一致（1×）。
- 源图 1487×1058 px，归一化为 1440×1024 后生成 `docs/redesign/comparison.png`，左源图右实际界面；约 0.06% 宽高比差异不作为视觉问题。
- focused region comparison: `docs/redesign/inspector-comparison.png`，同一输入内左右并排检查资料面板。
- state: 浅色、7 位虚构人物、选中盛国安。实际增加了人生经历内容和完整数据入口，图中生成的错误血缘连接不复刻。

## 比较结论

| 必查项 | 结果 |
|---|---|
| 字体与层级 | 系统中文无衬线，标题、节点姓名、辅助文字层级明确；源图未知字体以本机系统字体实现，未引入外部字体。实际截图文字无截断覆盖。 |
| 间距与布局 | 顶栏、宽画布、340px 右面板保留整体构图；移除旧左栏。面板内容独立滚动，主要操作固定底部。 |
| 色彩 | 暖白、灰绿线条、墨绿选中与操作，保留克制的细边界和阴影；取消性别色块。 |
| 资产 | 保留项目已有矢量品牌标识；源图临时“族”字标是有意替换。无照片或生成式装饰资产。 |
| 内容 | 名称、日期、亲属均来自实际数据。恢复点与导出入口可达，保存文案准确限定在本浏览器。 |

## 迭代与修复

1. 初次实际截图 `docs/redesign/desktop-before-qa.png`：P2，打开资料面板后画布偏移、缩放控件竖排、面板底部操作被内容挤出。
   修复：ResizeObserver 保持视口中心；提高控件样式优先级；资料区独立滚动并固定操作区。复查 `desktop-after-qa.png`。
2. 后续移动截图 `mobile-before-qa.png`：P2，窗口尺寸变化保留旧缩放导致节点被裁；适配后控制条可能覆盖末代节点。
   修复：窗口变化重新适配，使用非对称 padding 留出控制区；手机详情占位，避免覆盖画布。复查 `mobile.png`。
3. 最终桌面 1440×1024 再捕获 `desktop.png`，并与源图归一化并排比较；手机 `mobile.png` 390×844。上述 P2 均关闭，无待处理 P0/P1/P2 视觉问题。

## 实际交互与限制

实测人物选择、资料/亲属、展开添加亲属、阅读/关闭焦点恢复、编辑/取消、移动端适配和缩放。阅读截图 `reader.png`。手机完整家谱概览中的文字会随缩放变小，通过缩放、名录或搜索进入人物详情。

源图没有手机、空状态、错误状态的视觉设计，相关界面按同一视觉系统补齐；不主张这些状态与源图逐像素一致。此次为浏览器响应式验收，未声称真机触摸验收。

## 实现清单

- [x] 桌面构图、节点、资料面板和主要操作
- [x] 手机布局与视口尺寸变化
- [x] 独立阅读视图与焦点恢复
- [x] 源图/实际图全页和局部并排比较
- [x] 保持亲缘数据契约，明确本地存储边界

## 最终工程验证

- `npm test`：15 个测试文件 / 88 项通过。
- `npm run typecheck`、`npm run build`：通过。
- 实际 IndexedDB 存储浏览器测试：`STORAGE_BROWSER OK`（跨窗口冲突、原子导入回滚、恢复点 freshness、旧格式兼容）。
- 浏览器 `smoke-browser.mjs`：`SMOKE OK`。
- 浏览器 `qa-browser.mjs`：`QA PASS`（资料、日期、关系校验、JSON/Excel 往返、分享图、刷新和窄屏入口）。
- 生产构建 `http://127.0.0.1:4173/` 的 `e2e-browser.mjs`：`E2E PASS`，包括 1440 / 768 / 375 响应式截图。
- 生产预览实际打开并显示已有测试成员，未替换该浏览器原有数据；该页控制台 error 查询为空。
