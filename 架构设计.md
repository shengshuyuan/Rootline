# 族源 Rootline · 架构设计文档

| 项 | 内容 |
|---|---|
| 文档版本 | V0.1 |
| 日期 | 2026-07-12 |
| 状态 | MVP 历史设计基线；当前实现以 `docs/功能清单与下一步.md` 为准 |
| 配套文档 | 《盛氏家族族谱管理系统产品需求文档.docx》V0.1 |
| 适用阶段 | MVP（V0.1） |

> 本文档是技术架构层面的设计与决策沉淀，供开发实施和同行评审使用。产品层面的功能需求、数据模型字段定义以需求文档为准；本文档在需求文档基础上补充技术选型、分层结构、目录组织、布局方案与关键工程决策。

---

## 1. 项目概述

**族源 Rootline** 是一套纯前端、本地存储、单用户的数字族谱系统，用于将传统纸质族谱数字化为可持续维护的可视化亲缘关系图。

产品全称：**族源 Rootline · 盛氏家族数字族谱**
代码标识：`rootline`
标语：溯家族之源，续世代之脉。 / Trace your roots. Preserve your lineage.

MVP 的第一优先级（来自需求文档第 19 章）：

- 人物不重复
- 关系不混乱
- 数据不丢失
- 分支可持续扩展
- 族谱可以方便查看和维护

---

## 2. 需求边界（MVP 锁定项）

以下边界经产品讨论确认，作为开发 baseline。偏离需重新确认。

| 维度 | 锁定结论 |
|---|---|
| 部署形态 | 纯前端，无后端，数据存浏览器本地 |
| 用户模型 | 单用户，无登录、无权限、无协作 |
| 数据层 | 关系图模型（亲子关系、配偶关系均独立成实体表，不使用 `father_id` / `mother_id` 字段） |
| 配偶关系 | **MVP 每人最多 1 个配偶**；创建、关联和导入均校验并明确拒绝第二条配偶关系。数据模型保留多配偶扩展空间，但本期不渲染、不静默丢弃多配偶数据 |
| 画布展示范围 | 默认展示「中心人物 + 上一代 + 下一代」，可手动展开更多 |
| 添加方式 | 一辈一辈加，每次操作只增加 1 人 / 连接 1 条关系，不做批量录入 |
| 同人画布表现 | 每个被展示的人在画布上只画一个节点；归位规则 = 离中心人物最近，平局时优先原生家庭 |
| 多配偶叠加布局 | ❌ 不处理（推迟） |
| 远距离跨支联姻线 | ❌ 不专门优化（保证数据正确即可，视觉从简） |
| 同宗族内联姻 | 基本不存在，向下 3 代范围内不会出现循环冲突 |
| 视图模式 | 主脉视图（亲生父系中男性的儿子、女儿均保留；女性后代不展开）与完整亲缘视图切换；切换只影响展示，不改数据 |
| 数据持久化 | IndexedDB 存族谱数据，LocalStorage 存 UI 设置，JSON 存导入导出 |
| 数据规模目标 | 100 人内主要操作流畅；500 人内画布可正常拖动缩放 |

---

## 3. 技术栈

全部为开源、免费、可商用许可。

| 类别 | 选型 | 许可证 | 选型理由 |
|---|---|---|---|
| 框架 | React 18 + TypeScript | MIT | 需求文档指定；类型系统对关系模型的安全性至关重要 |
| 构建 | Vite | MIT | 启动快、配置简，适合 MVP |
| 画布引擎 | React Flow（`@xyflow/react`） | MIT | 仅用作画布交互层（拖拽/缩放/连线/视口），节点为 React 组件，便于做富交互人物卡片；官方承诺永久 MIT |
| 状态管理 | React Hooks + 应用控制 hooks | MIT | 保持依赖精简；当前单页、本地优先场景无需额外状态框架 |
| 本地存储 | Dexie.js（IndexedDB 封装） | Apache-2.0 | 封装原生事务，API 接近对象操作 |
| 样式 | 原生 CSS + 设计变量 | W3C 标准 | 与当前实现一致，避免未使用的构建依赖并保持视觉统一 |
| 单元测试 | Vitest | MIT | 关系校验层、布局器必须独立可测 |
| ID 生成 | nanoid | MIT | 比 UUID 短，唯一性足够 |

### 品牌视觉规范（来自开发技能约定，全项目固定）

- 强调色：`#FF642B`（亮橙），仅用于点缀（主按钮、选中态、链接、重点标记）；大面积使用中性色
- 字体：英文 Inter / 中文 Noto Sans SC / 代码 JetBrains Mono
- 设计取向：工具型应用，克制、信息密度合理，不堆砌视觉装饰

### 关于画布引擎的决策说明

族谱布局**不能使用任何通用图布局算法**：

| 通用布局 | 问题 |
|---|---|
| 力导向（force-directed） | 代际信息丢失，族谱变成无方向「星系图」 |
| 分层 dagre | 不让配偶并排同层；子女坐标不对齐父母中点；多配偶崩溃 |
| tidy tree（d3-tree） | 单根树模型，每人只能有一个父节点，无法表达「父+母」双亲与配偶横向关系 |

因此决定：**React Flow 仅承担画布交互与渲染，所有节点坐标由自研布局器计算后注入，不使用 React Flow 的自动布局。** 开源社区无现成的「族谱画布」组件可复用（参考项 Gramps Web 是含后端的完整应用，非可复用画布）。

---

## 4. 分层架构

呼应需求文档 12.4 节的代码分层要求，并落实 Repository 接口抽象。

```
┌──────────────────────────────────────────────────┐
│ 1. UI 展示层 presentation                         │
│    React 组件 + React Flow 画布                    │
│    画布·人物节点卡片·详情面板·工具栏·表单·空状态     │
│    职责：只管「怎么画」，不含业务逻辑               │
├──────────────────────────────────────────────────┤
│ 2. 状态与应用层 state (React hooks/controller)     │
│    UI 状态：当前选中人物、中心人物、视图模式、        │
│    折叠状态、保存指示、画布视口                     │
│    职责：把 UI 意图交给领域服务，保存服务返回的状态    │
├──────────────────────────────────────────────────┤
│ 3. 业务逻辑层 domain (纯 TS，不依赖 React 或 Dexie) │
│    PersonService / RelationshipService            │
│    LayoutEngine：展示树生成 + 坐标计算             │
│    职责：增删改查编排，调用校验层；只依赖仓储接口     │
├──────────────────────────────────────────────────┤
│ 4. 关系校验层 validation (纯函数)                 │
│    唯一性·循环检测·重复人物·日期合法性·性别角色     │
│    职责：独立的、可脱离 UI 单测的规则集合           │
├──────────────────────────────────────────────────┤
│ 5. 仓储端口 repository (接口抽象)                  │
│    FamilyTreeRepository：load/save/replaceFromImport/export │
│    职责：定义领域层所需的数据读写契约，不含 Dexie    │
├──────────────────────────────────────────────────┤
│ 6. 基础设施层 infrastructure / storage             │
│    IndexedDbFamilyTreeRepository (Dexie，适配端口)  │
│    LocalStorage（UI 设置）                         │
│    JSON 文件（导入/导出）                          │
│    未来：ApiFamilyTreeRepository                  │
└──────────────────────────────────────────────────┘
```

### 依赖规则（强制）

1. 调用链固定为：**UI/Store → Domain Service → `FamilyTreeRepository` 接口 → IndexedDB/JSON 等基础设施实现**；布局器为领域层纯函数，React Flow 只消费其结果。
2. 第 3、4 层必须是纯 TypeScript，不 import React、Dexie 或 IndexedDB；领域服务可以 import `FamilyTreeRepository` 的 TypeScript 接口，不能 import 任何具体实现。
3. UI、Store 与 React 组件不得直接调用 Dexie、IndexedDB 或 `IndexedDbFamilyTreeRepository`；读取、写入、导入替换和导出都由领域服务经仓储端口完成。
4. `IndexedDbFamilyTreeRepository` 与未来的 `ApiFamilyTreeRepository` 都是第 5 层接口的适配器；替换存储实现不应改变 UI、Store、领域校验或布局模块。

---

## 5. 目录结构（规划）

```
rootline/
├── src/
│   ├── types/              # 数据模型类型
│   │   ├── person.ts
│   │   ├── relationship.ts
│   │   ├── date.ts
│   │   └── familyTree.ts
│   ├── validation/         # 关系校验层（纯函数）
│   │   ├── uniqueness.ts   # 唯一性（人物 ID、关系不重复）
│   │   ├── cycle.ts        # 祖先循环检测
│   │   ├── duplication.ts  # 疑似重复人物
│   │   └── dateRules.ts    # 日期合法性（去世不早于出生等）
│   ├── domain/             # 业务逻辑层（纯 TS）
│   │   ├── services/
│   │   │   ├── personService.ts
│   │   │   └── relationshipService.ts
│   │   ├── layout/
│   │   │   ├── displayTree.ts   # 关系图 → 展示树（去重 + 归位）
│   │   │   ├── positioner.ts    # 展示树 → 坐标（简化 Walker）
│   │   │   └── types.ts
│   │   └── search.ts
│   ├── repository/         # 仓储端口（仅接口与数据契约）
│   │   └── FamilyTreeRepository.ts
│   ├── infrastructure/     # 仓储适配器与本地基础设施
│   │   ├── IndexedDbFamilyTreeRepository.ts
│   │   └── migration.ts              # schemaVersion 迁移
│   ├── storage/            # 本地存储细节
│   │   ├── db.ts                    # Dexie 实例定义
│   │   └── localStorage.ts          # UI 设置持久化
│   ├── io/                # 导入导出
│   │   ├── exportJson.ts
│   │   ├── importJson.ts
│   │   └── schemaValidate.ts        # 导入文件结构校验
│   ├── hooks/             # 应用状态与数据操作 hooks
│   │   ├── useTreeController.ts     # 族谱数据与保存编排
│   │   └── useUiState.ts            # UI 状态（按需拆分）
│   ├── components/         # UI 展示层
│   │   ├── canvas/         # React Flow 画布
│   │   │   ├── FamilyCanvas.tsx
│   │   │   ├── PersonNode.tsx
│   │   │   └── edges/
│   │   ├── panels/         # 右侧详情、左侧统计
│   │   ├── toolbar/        # 顶部工具栏
│   │   ├── forms/          # 创建/编辑人物表单
│   │   ├── empty/          # 空状态
│   │   └── common/         # 通用组件
│   ├── utils/
│   ├── App.tsx
│   └── main.tsx
├── tests/                  # 单元测试（与 src 同结构）
├── public/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── vite.config.ts
```

---

## 6. 数据模型（要点）

字段定义详见需求文档第 9 章。本节只记录技术决策性的补充：

| 实体 | 关键决策 |
|---|---|
| `Person` | `id` 用 nanoid；`createdAt` / `updatedAt` 用 ISO 8601 字符串 |
| `DateRecord` | 同时保存 `value` + `precision` + `calendar` + `originalText`，支持「约民国二十四年」这类模糊日期；农历转换推迟到 P1 |
| `ParentChildRelationship` | 独立实体，不退化成 `father_id`；`relationshipType` 预留 biological/adopted/step/guoji/unknown，MVP 仅用 biological 和 unknown |
| `PartnerRelationship` | 不区分方向（A 配偶 B 等价于 B 配偶 A），去重时双向判断；表结构支持多条，MVP UI 拦截第二配偶 |
| `FamilyTree` | 持有 `persons` + `parentChildRelationships` + `partnerRelationships` 三个集合；`schemaVersion` 从 `1.0.0` 起（semver） |
| `DisplayTree` / `LayoutNode` | 视图层临时结构，不持久化，仅用于画布渲染 |

---

## 7. 画布布局方案（核心技术模块）

### 7.1 关键抽象：家庭单元

布局的原子单位不是「人」，而是「家庭单元」（需求文档 5.4 已定义）：

> 家庭单元 = { 一对配偶，他们的共同子女 }

一个人作为子女时属于「原生家庭单元」，作为配偶/父母时属于「婚姻家庭单元」。在画布上，这个人**画在他的原生家庭单元位置**，婚姻关系通过线连接到配偶所在单元。

### 7.2 两层架构

```
┌──────────────────────────────────────────────────┐
│ 布局计算器（纯函数）                                │
│   输入：关系图 + 中心人物 + 展示范围 + 视图模式      │
│   输出：personId → { x, y } 坐标表                 │
│   内部：                                            │
│     步骤 A — DisplayTreeBuilder：                  │
│       关系图 → 展示树（去重 + 就近归位）            │
│     步骤 B — Positioner：                          │
│       展示树 → 坐标（家庭单元分组 + 简化 Walker）   │
└──────────────────────────────────────────────────┘
                      ↓ 注入坐标
┌──────────────────────────────────────────────────┐
│ React Flow（只渲染，不布局）                        │
│   拖拽 / 缩放 / 视口 / 连线 / 选中                  │
│   人物节点 = React 组件                            │
└──────────────────────────────────────────────────┘
```

拆成 A、B 两步是为了可调试性：当「某个人画在了奇怪的位置」时，能分别检查是去重归位错了，还是坐标算错了。

### 7.3 展示树去重规则（同人单节点）

- 展示树以「当前中心人物」为根，有上下方向，**切换中心人物时整树重算**。
- 遍历关系图时，每个人只被放入展示树一次（去重）。
- 归位规则：画在「离中心人物最近的那条路径上」；距离相等时，**优先画在原生家庭**，其婚姻家庭作为向下分支展开。

### 7.4 代际计算

使用**相对代际**（不依赖需求文档 `generation` 字段是否已填）：

- 中心人物 = 第 0 代
- 每经过一个父 / 母，代数 +1（向上）
- 每经过一个子女，代数 -1（向下）

### 7.5 MVP 简化策略

- 只布局中心人物的局部树，不做全图。
- 默认展示范围：中心人物 + 上一代 + 下一代，可手动展开。
- 远距离跨支联姻线用直线/折线连接，不追求视觉优雅。
- 多配偶叠加不处理。
- 不实现完整的交叉最小化，使用简化版 Walker。

### 7.6 布局目标形态

```
              [祖父]═══[祖母]              ← 第 +2 代，═══ 婚姻线
                  ║                          ║ 父子线，居中下挂
            ┌────╨────┐
         [父亲]═══[母亲]                   ← 第 +1 代，配偶并排
              ║
      ┌───────╨────────┬─────────┐
   [本人]══[配偶]  [兄弟]    [女儿]═══[女婿]   ← 第 0 代
                                   ║
                               ▒▒不纳入▒▒       ← 主脉视图：女性后代不纳入
                               (已折叠 N 人)
```

---

## 8. 数据流示例

以「为中心人物添加一位父亲」为例：

```
用户点击「添加父亲」→ 填写表单 → 提交
    → Store 调用 PersonService.createParent(currentId, 'father', formData)
    → validation 校验：
        - 唯一性（不重复创建同一亲子关系）
        - 循环检测（新父亲不能是当前人物的后代）
        - 重复人物检测（姓名+性别+生年近似）
    → 校验通过
      → FamilyTreeRepository.save(newTree)  // 由注入的适配器写 IndexedDB
        → PersonService 返回更新后的树
          → React 应用状态更新       // hooks/controller 状态更新
          → LayoutEngine 重算坐标
            → React Flow 重渲染
              → 保存指示器：保存中 → 已保存
```

---

## 9. 状态管理设计

| 状态域 | 职责 | 持久化 |
|---|---|---|
| 族谱数据状态 | 当前族谱数据（persons、relationships）、当前 treeId | IndexedDB（经 repository） |
| UI 状态 | 选中人物、中心人物、视图模式、折叠状态、保存指示、画布视口 | LocalStorage（部分） |

自动保存触发时机（需求文档 8.15）：创建/编辑人物、建立/解除关系、删除人物、修改族谱设置。每次写操作后异步保存，UI 显示「保存中 / 已保存 / 保存失败」，不阻塞用户操作。

---

## 10. 安全（落实需求文档 12.3）

- 数据默认只存本地 IndexedDB，**应用本身不发起任何外部数据请求**（仅字体 CDN 和 npm 包加载）。
- 不上传任何家族数据。
- 导出 JSON 文件由用户自行保管，应用提示定期备份。
- 生辰八字、出生日期等敏感信息仅存本地，无默认公开风险。
- 未来上线 Web 版时再加 CSP 等加固。

---

## 11. 性能目标（落实需求文档 12.1）

| 指标 | 目标 |
|---|---|
| 100 人内主要操作 | 流畅 |
| 500 人内画布 | 可正常缩放拖动（必要时开 React Flow 节点虚拟化，只渲染视口内节点） |
| 添加人物后布局更新 | < 1 秒 |
| 搜索响应 | < 300 ms |

布局计算是纯函数，若 500 人量级出现卡顿，可迁入 Web Worker 执行，不改变接口。

---

## 12. 预留扩展点

| 扩展点 | 设计预留 |
|---|---|
| 后端化 | `FamilyTreeRepository` 接口，加 `ApiFamilyTreeRepository` 实现即可，上层不动 |
| 多家族 | `FamilyTree` 已有 `id` / `name`，未来支持多 tree 切换 |
| 多配偶 | `PartnerRelationship` 数据结构允许未来扩展；届时需同步放开创建/关联/导入校验并实现对应布局与交互，不能仅移除 UI 拦截 |
| Schema 升级 | `schemaVersion` + 迁移链；加载与导入先迁移再校验，迁移前自动创建恢复点 |
| 协作 / 权限 | 推迟到 V0.3，当前架构不阻塞 |

---

## 13. 开发阶段拆分（来自需求文档第 14 章）

| 阶段 | 内容 | 验收 |
|---|---|---|
| 一 | 数据模型 + 关系校验 + JSON 导入导出 + IndexedDB 存储 | 用测试数据可正确增删查人物关系（无 UI） |
| 二 | 族谱画布：人物节点、关系线、自动布局、缩放拖动、选中、详情面板、添加父母配偶子女 | 录入并查看 20 人 4 代测试族谱 |
| 三 | 编辑人物、关联已有人物、解除关系、删除人物、家庭单元归属、重复人物提醒 | 正确处理旁支连接与重名 |
| 四 | 搜索定位、分支折叠、主脉/完整视图、错误提示、空状态、自动保存指示 | 普通用户无指导可完成基础录入 |

严格按阶段顺序推进。数据层是命根子，画布是脸面，先数据后画布。

---

## 14. 待评审 / 待定项

以下点欢迎评审时重点反馈：

1. **状态管理**：当前继续使用 React Hooks；只有出现跨页面共享和明显状态耦合时再评估额外框架。
2. **布局器拆分**：DisplayTreeBuilder 与 Positioner 的边界是否合理？是否应进一步拆出「家庭单元分组」独立步骤？
3. **React Flow 节点虚拟化**：是否应在阶段二就引入，还是等 500 人量级再优化？
4. **schemaVersion 迁移机制**：已建立版本迁移链、导出信封和迁移前恢复点；新增版本时必须补迁移与兼容测试。
5. **日期农历**：`DateRecord.calendar` 字段已留，但农历换算推迟到 P1，是否影响 MVP 数据一致性？
6. **测试数据**：需求文档第 15 章要求的虚拟测试族谱，是否应作为阶段一的交付物（JSON fixture）而非阶段二？
